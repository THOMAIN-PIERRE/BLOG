{% extends 'base.html.twig' %}

{% block title %}Mon Compte{% endblock %}

{% block body %}

<div class="container">
<div class="col align-items-center">
<div class="text-center">
<img src="https://img2.freepng.fr/20180615/rtc/kisspng-avatar-user-profile-male-logo-profile-icon-5b238cb002ed52.870627731529056432012.jpg" alt="avatar" class="avatar mb-3 mt-5" alt="Avatar">
</div>
<div class="text-center">
<h1>{{user.username}}</h1>
{% if user is same as(app.user) %}

	<div class="mt-3">
	{# <a href="{{ path('account_profile') }}" class="btn btn-primary btn-lg mr-2 mb-2">Modifier le profil</a> #}
	<a href="{{ path('account_password') }}" class="btn btn-primary btn-lg mr-2 mb-2"><i class="fas fa-lock"></i> Modifier mon mot de passe</a>
	</div>
	<hr style="background-color: black; width: 70%; height: 3px">
{% endif %}
</div>
</div>
</div>



{# <div class="container-fluid text-center mt-5 mb-5">
	<h1 id="haut" class="my-5 mt-5">Gestionnaire de compte</h1>
    <a href="#profil" class="col-2 btn btn-primary btn-lg ml-2"><i class="fas fa-user"></i> Mon profil</a>
	<a href="#commandes" class="col-2  btn btn-primary btn-lg ml-2"><i class="fas fa-list-ul"></i> Mes publications</a>
</div> #}

<div class="container-fluid bg-light">	
		<h3 id="profil" class="my-5 text-center" style="font-weight: bold; border: 2px solid Black">Mon profil</h1>
		<table class="table table-hover">
			<thead>
				<tr class="col bg-primary text-light">
					<th class="text-center align-middle">NOM UTILISATEUR</th>
					<th class="text-center align-middle">EMAIL</th>
					{# <th class="text-center align-middle">MOT DE PASSE</th> #}
					{# <th class="text-center align-middle">Date d'inscription</th> #}
					{# <th class="text-center align-middle">Validation CGU</th> #}
                    <th class="text-center align-middle">ACTIONS</th>
				</tr>
			</thead>
            <tbody>
                <tr class="col bg-secondary text-light font-weight-bold">
                    <td class="text-center align-middle">{{user.username}}</td>
                    <td class="text-center align-middle">{{user.email}}</td>
                    {# <td class="text-center align-middle">{{user.password |slice(10,20)|raw}}</td> #}
					{# <td class="text-center align-middle">{{user.password|slice(0,40)|raw}}</td> #}
					{# <td class="text-center align-middle">
					<div class="text-center align-middle"><img src="{{user.avatar}}" alt="avatar" class="avatar-medium mb-3 mt-5" alt="Avatar">
					</div></td>  #}
                    {# <td class="text-center align-middle">{{user.date_Inscription|date("d/m/Y")}}</td> #}
					{# <td class="text-center align-middle">{{user.acceptation_CGU}}</td> #}
					<td class="text-center align-middle">
					<a href="{{ path('account_profile') }}" class="col-4 btn btn-success mr-2 mb-2"><i class="fa fa-pencil-square" aria-hidden="true"></i> Modifier</a>
					{# <a href="" class="col-4 btn btn-success mr-2 mb-2"><i class="fas fa-lock"></i> Supprimer</a> #}
                    
                    </td>
                    
                </tr>
            </tbody>
		<table>
    </div>
	
<hr>

{# <hr> #}

{# <div class="container-fluid">
		
		<h3 id="commandes" class="my-5 text-center" style="font-weight: bold; border: 2px solid Black"> Historique de mes publications d'articles</h1>
		<table class="table table-hover">
			<thead>
				<tr class="col bg-primary text-light">
					<th class="text-center align-middle">TITRE</th>
					<th class="text-center align-middle">CONTENU</th>
					<th class="text-center align-middle">IMAGE</th>
                    <th class="text-center align-middle">DATE DE PUBLICATION</th>
					<th class="text-center align-middle">ACTIONS</th>
				</tr>
			</thead> #}
            {# <tbody> #}
			{# {% for article in articles %} #}
                {# <tr class="col bg-secondary text-light font-weight-bold"> #}
                    {# <td class="text-center align-middle">{{user.article.title}}</td>
                    <td class="text-center align-middle">{{user.article.content}}</td>
                    <td class="text-center align-middle">{{user.article.image}}</td>
                    <td class="text-center align-middle">{{user.article.createdAt}}</td>
                    <td class="text-center align-middle">
						<a href="{{ path('account_profile', {'id': article.id}) }}" class="col-4 btn btn-success mr-2 mb-2"><i class="fas fa-user"></i> Modifier</a> #}
						{# <a href="{{ path('account_profile') }}" class="col-4 btn btn-success mr-2 mb-2"><i class="fas fa-lock"></i> Supprimer</a> </td>  #}
                {# </tr> #}
			{# {% endfor %}
            </tbody> #}
		{# <table>
    </div>
	
<hr>  #}

	{# <div class="container-fluid text-center mt-5 mb-5">
	<a href="#haut" class="btn btn-success ml-4"><i class="fas fa-arrow-circle-up"></i> Haut de page</a>
	</div>   #}
{% endblock %}



